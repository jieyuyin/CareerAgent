import JobCard from './JobCard.jsx'
export default function JobSection({ title, jobs, ...props }) { if (!jobs.length) return null; return <section className="mt-9"><div className="flex items-center gap-3 px-4"><h2 className="text-sm font-semibold">{title}</h2><span className="text-xs text-stone-400">{jobs.length}</span><span className="h-px flex-1 bg-black/10"/></div><div className="mt-2">{jobs.map((job) => <JobCard key={job.id} job={job} {...props} selected={props.selectedId === job.id} favored={props.preferences.has(job.id)} onSelect={() => props.onSelect(job.id)} onFavorite={() => props.onFavorite(job.id)} onDismiss={() => props.onDismiss(job.id)} />)}</div></section> }
