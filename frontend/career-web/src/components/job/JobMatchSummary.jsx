import JobEvidenceList from './JobEvidenceList.jsx'
export default function JobMatchSummary({ report }) {
  if (!report) return null
  return <section className="mt-7"><div className="flex items-end justify-between"><div><p className="text-xs text-stone-400">AI 辅助匹配</p><p className="mt-1 font-serif text-5xl tracking-tight">{report.matchScore}<span className="ml-1 text-xl text-stone-400">%</span></p></div><p className="max-w-32 text-right text-[11px] leading-4 text-stone-400">参考结果，不替代你的最终判断</p></div><div className="mt-6"><p className="text-xs font-semibold text-stone-500">已命中能力</p><div className="mt-2 flex flex-wrap gap-1.5">{report.matchedSkills?.map((skill) => <span key={skill} className="rounded-md bg-[#dcecdf] px-2 py-1 text-[11px] text-[#315d3b]">{skill}</span>)}</div></div><div className="mt-5"><p className="text-xs font-semibold text-stone-500">能力缺口</p><div className="mt-2 flex flex-wrap gap-1.5">{report.missingSkills?.length ? report.missingSkills.map((skill) => <span key={skill} className="rounded-md bg-[#eeeae2] px-2 py-1 text-[11px] text-stone-600">{skill}</span>) : <span className="text-xs text-stone-400">暂无明确缺口</span>}</div></div><div className="mt-6"><JobEvidenceList evidence={report.evidence}/></div></section>
}
