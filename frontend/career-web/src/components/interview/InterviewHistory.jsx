import { PanelHeader } from './InterviewConfigPanel.jsx';import {stageLabels} from './interviewLabels.js'
export default function InterviewHistory({items=[],loading,onSelect,onBack}){return <div><button onClick={onBack} className="mb-5 text-xs text-stone-500">← 返回新建</button><PanelHeader eyebrow="Past sessions" title="历史模拟面试"/>{loading&&<div className="mt-6 h-36 animate-pulse rounded-xl bg-black/5"/>}<div className="mt-6 divide-y divide-black/10">{items.map(item=><button key={item.id} onClick={()=>onSelect(item.id)} className="block w-full py-4 text-left"><div className="flex justify-between gap-3"><p className="text-sm font-semibold leading-5">{item.company?`${item.company} · ${item.job}`:'通用模拟面试'}</p>{item.score!=null&&<span className="text-sm font-bold text-[#315d3b]">{item.score}分</span>}</div><div className="mt-2 flex justify-between text-xs text-stone-400"><span>{stageLabels[item.stage]} · {item.status==='FINISHED'?'已完成':'进行中'}</span><span>{new Date(item.createdTime).toLocaleDateString('zh-CN')}</span></div></button>)}</div>{!loading&&!items.length&&<p className="py-12 text-center text-sm text-stone-400">还没有历史模拟面试</p>}</div>}
