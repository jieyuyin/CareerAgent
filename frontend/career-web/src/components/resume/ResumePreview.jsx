export default function ResumePreview({ resume }) { return <article className="bg-[#fffdf8] px-10 py-10 shadow-[0_12px_40px_rgba(34,30,24,.08)]"><div className="border-b border-black/15 pb-7"><h2 className="font-serif text-3xl">{resume.fullName}</h2><p className="mt-2 text-sm text-stone-500">{[resume.targetRole,resume.phone,resume.email,resume.location].filter(Boolean).join(' · ')}</p></div><Block title="教育经历" items={resume.education} primary="school" secondary="major"/><Block title="实习经历" items={resume.experiences} primary="company" secondary="position"/><Block title="项目经历" items={resume.projects} primary="name" secondary="role"/><section className="mt-8"><h3 className="text-xs font-bold uppercase tracking-[.16em] text-stone-400">技能</h3><div className="mt-3 flex flex-wrap gap-2">{(resume.skills||[]).map(skill=><span key={skill} className="rounded-full bg-[#e8eee8] px-3 py-1.5 text-xs">{skill}</span>)}</div></section></article> }
function Block({ title, items=[], primary, secondary }) { if(!items.length)return null; return <section className="mt-8"><h3 className="text-xs font-bold uppercase tracking-[.16em] text-stone-400">{title}</h3><div className="mt-4 space-y-5">{items.map((item,index)=><div key={index}><div className="flex justify-between"><b>{item[primary]||'待确认'}</b><span className="text-xs text-stone-400">{item.period}</span></div><p className="mt-1 text-sm text-stone-600">{item[secondary]}</p>{item.description&&<p className="mt-2 whitespace-pre-line text-sm leading-6 text-stone-600">{item.description}</p>}</div>)}</div></section> }
