import { useState } from 'react'
import ResumeEditor from './ResumeEditor.jsx'
const steps=['基本信息','教育经历','实习经历','项目经历','技能']
export default function ResumeCreateWizard({ initial, onSave, saving, title='从零创建基础简历' }) { const [value,setValue]=useState(initial); const [step,setStep]=useState(0); const invalid=!value.fullName?.trim(); const saveButton=<button disabled={saving||invalid} onClick={()=>onSave(value)} className="rounded-xl bg-black px-7 py-3 text-sm font-semibold text-white disabled:cursor-not-allowed disabled:opacity-40">{saving?'保存中…':'确认并保存为基础简历'}</button>; return <div><div className="flex items-start justify-between gap-6"><div><h2 className="font-serif text-3xl">{title}</h2><p className="mt-2 text-sm text-stone-500">确认内容后即可保存；标记为“待确认”的字段可以保存后继续编辑。</p></div>{saveButton}</div>{invalid&&<p className="mt-4 rounded-lg bg-red-50 px-4 py-3 text-sm text-red-700">请先填写姓名，再保存为基础简历。</p>}<div className="my-7 flex gap-2">{steps.map((name,index)=><button type="button" key={name} onClick={()=>setStep(index)} className={`rounded-full px-3 py-1.5 text-xs ${index===step?'bg-black text-white':'bg-black/5 text-stone-500'}`}>{index+1}. {name}</button>)}</div><ResumeEditor value={value} onChange={setValue}/><div className="mt-10 flex justify-end border-t border-black/10 pt-6">{saveButton}</div></div> }
