export default function ResumeVersionList({resume,versions=[],selected,onSelect,onExport,exporting}){return <section className="mt-10 border-t border-black/10 pt-7"><div className="flex items-center justify-between"><div><p className="text-[10px] font-bold uppercase tracking-[.18em] text-stone-400">Versions</p><h2 className="mt-2 font-serif text-2xl">简历版本</h2></div><span className="text-xs text-stone-400">{versions.length+1} 个版本</span></div><div className="mt-5 grid grid-cols-3 gap-3"><Version active={selected==='master'} name="Master Resume" type="基础简历" time={resume?.updatedAt} onClick={()=>onSelect('master')} onExport={()=>onExport(null,true)} exporting={exporting==='master'}/>{versions.map(v=><Version key={v.id} active={selected===v.id} name={v.versionName} type="岗位定制" time={v.createdAt} onClick={()=>onSelect(v.id)} onExport={()=>onExport(v.id,false)} exporting={exporting===v.id}/>)}</div></section>}
function Version({active,name,type,time,onClick,onExport,exporting}){return <div onClick={onClick} className={`cursor-pointer rounded-2xl border p-4 transition ${active?'border-black bg-[#eee9df]':'border-black/10 bg-white/50 hover:border-black/30'}`}><div className="flex items-start justify-between gap-2"><div><b className="text-sm">{name}</b><p className="mt-1 text-xs text-stone-500">{type}</p></div><span className="rounded-full bg-[#dfe9e1] px-2 py-1 text-[10px] text-[#315d3b]">{type==='基础简历'?'MASTER':'CUSTOM'}</span></div><div className="mt-5 flex items-center justify-between"><time className="text-[10px] text-stone-400">{time?new Date(time).toLocaleDateString('zh-CN'):'—'}</time><button onClick={e=>{e.stopPropagation();onExport()}} className="text-xs font-semibold underline decoration-black/20 underline-offset-4">{exporting?'生成中…':'导出 PDF'}</button></div></div>}
