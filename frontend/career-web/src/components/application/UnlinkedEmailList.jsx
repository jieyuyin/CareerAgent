import {useState} from 'react'
export default function UnlinkedEmailList({emails=[],applications=[],onCreate,onLink,onReanalyze,busy}){const [choices,setChoices]=useState({});if(!emails.length)return null;return <section className="mt-8 border-y border-amber-900/15 bg-[#f4eddd] px-6 py-5"><div className="flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-[.15em] text-amber-800/60">Needs confirmation</p><h2 className="mt-1 font-serif text-xl">待关联招聘邮件</h2></div><span className="rounded-full bg-amber-100 px-3 py-1 text-xs text-amber-800">{emails.length} 封</span></div><div className="mt-4 space-y-3">{emails.map(email=><div key={email.id} className="grid grid-cols-[minmax(0,1fr)_220px_auto] items-center gap-3 rounded-xl bg-white/65 p-4"><div><p className="text-sm font-semibold">{email.subject}</p><p className="mt-1 text-xs text-stone-500">{email.company||'公司待确认'} · {email.jobName||'岗位待确认'} · {email.emailType}</p></div><select className="input py-2 text-xs" value={choices[email.id]||''} onChange={e=>setChoices({...choices,[email.id]:e.target.value})}><option value="">选择已有投递</option>{applications.map(app=><option key={app.id} value={app.id}>{app.company} · {app.jobName}</option>)}</select><div className="flex gap-2"><button disabled={!choices[email.id]||busy} onClick={()=>onLink(email.id,Number(choices[email.id]))} className="rounded-lg border border-black/15 px-3 py-2 text-xs font-semibold">关联</button><button onClick={()=>onCreate({emailId:email.id,subject:email.subject,company:email.company,jobName:email.jobName})} className="rounded-lg bg-black px-3 py-2 text-xs font-semibold text-white">新建进展</button><button disabled={busy} onClick={()=>onReanalyze(email.id)} className="px-2 text-xs text-stone-500 underline">重新识别</button></div></div>)}</div></section>}
