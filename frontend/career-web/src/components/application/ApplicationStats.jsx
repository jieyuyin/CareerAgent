export default function ApplicationStats({data,loading}){const items=[['全部记录',data?.total],['进行中',data?.inProgress],['已获 Offer',data?.offers],['已拒绝',data?.rejected],['本月投递',data?.thisMonth]];return <section className="mt-10 grid grid-cols-5 border-y border-black/10">{items.map(([label,value],index)=><div key={label} className={`py-6 ${index?'border-l border-black/10 pl-6':''}`}><p className="text-xs text-stone-400">{label}</p><p className="mt-2 text-3xl font-semibold tracking-tight">{loading?'—':value??0}</p></div>)}</section>}
