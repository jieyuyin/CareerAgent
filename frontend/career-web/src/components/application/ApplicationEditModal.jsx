import {useEffect,useState} from 'react';import {interviewStageLabels,statusLabels} from './applicationLabels.js'
export default function ApplicationEditModal({application,onClose,onSave,saving,error}){const [form,setForm]=useState({status:'APPLIED',interviewStage:null,nextAction:'',remark:''});useEffect(()=>{if(application)setForm({status:application.status,interviewStage:application.interviewStage||'TECH_ONE',nextAction:application.nextAction||'',remark:application.remark||''})},[application]);if(!application)return null;const status=value=>setForm({...form,status:value,interviewStage:value==='INTERVIEW'?(form.interviewStage||'TECH_ONE'):null});return <div className="fixed inset-0 z-50 grid place-items-center bg-black/25 p-6" onMouseDown={event=>event.target===event.currentTarget&&onClose()}><div className="w-full max-w-lg rounded-2xl bg-[#faf8f2] p-7 shadow-2xl"><div className="flex justify-between"><div><p className="text-xs text-stone-400">{application.company}</p><h2 className="mt-1 font-serif text-2xl">编辑求职进展</h2></div><button onClick={onClose} className="text-stone-400">关闭</button></div><div className="mt-7 space-y-5"><Field label="当前状态"><select className="input" value={form.status} onChange={event=>status(event.target.value)}>{Object.entries(statusLabels).map(([value,label])=><option key={value} value={value}>{label}</option>)}</select></Field>{form.status==='INTERVIEW'&&<Field label="面试轮次"><select className="input" value={form.interviewStage||'TECH_ONE'} onChange={event=>setForm({...form,interviewStage:event.target.value})}>{Object.entries(interviewStageLabels).map(([value,label])=><option key={value} value={value}>{label}</option>)}</select></Field>}<Field label="下一步事项"><input className="input" maxLength="500" value={form.nextAction} onChange={event=>setForm({...form,nextAction:event.target.value})} placeholder="例如：准备业务二面"/></Field><Field label="备注"><textarea rows="4" className="input resize-y" value={form.remark} onChange={event=>setForm({...form,remark:event.target.value})} placeholder="记录进展、联系人或待办信息"/></Field></div>{error&&<p className="mt-4 text-sm text-red-700">保存失败：{error.message}</p>}<div className="mt-7 flex justify-end gap-3"><button onClick={onClose} className="rounded-xl border border-black/10 px-5 py-3 text-sm">取消</button><button disabled={saving} onClick={()=>onSave(form)} className="rounded-xl bg-black px-6 py-3 text-sm font-semibold text-white disabled:opacity-40">{saving?'保存中…':'保存修改'}</button></div></div></div>}
function Field({label,children}){return <label className="block"><span className="text-sm font-semibold">{label}</span><div className="mt-2">{children}</div></label>}
